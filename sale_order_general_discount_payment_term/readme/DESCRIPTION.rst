This module allows to set a general discount in a payment term.
This discount will be calculated on a sale order, when that payment term
is selected.

If payment term selected hasn't a discount, but partner has, will
apply that one.
