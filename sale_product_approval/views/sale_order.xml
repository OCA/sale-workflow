<odoo>

    <record id="sale_order_inherit_form" model="ir.ui.view">
        <field name="name">sale_order_form_view_inherit</field>
        <field name="model">sale.order</field>
        <field name="priority">99</field>
        <field name="inherit_id" ref="sale.view_order_form" />
        <field name="arch" type="xml">
            <xpath
                expr="//field[@name='order_line']//tree/field[@name='product_template_id']"
                position="after"
            >
                <button
                    invisible="approved_sale_confirm or state in ('sale','done','cancel')"
                    class="fa fa-exclamation-triangle"
                    title="Product has been marked as not saleable."
                />
                <field column_invisible="True" name="approved_sale_confirm" />
            </xpath>
            <div name="button_box" position="after">
                <field invisible="1" name="exceptions_sale_approval_confirm" />
                <div
                    invisible="not exceptions_sale_approval_confirm or state in ('sale','done','cancel')"
                    class="alert alert-warning text-center"
                    role="alert"
                >Exception: A product on this sale order has been marked as not saleable.</div>
            </div>
            <field name="client_order_ref" position="after">
                <field
                    name="override_saleable_exception"
                    groups="sales_team.group_sale_manager"
                />
            </field>
            <button id="action_confirm" position="replace" />
            <button name="action_confirm" position="attributes">
                <attribute name="context">{'override_ex': True}</attribute>
            </button>
            <button name="action_confirm" position="after">
                <button
                    name="action_confirm"
                    id="action_confirm"
                    string="Confirm"
                    class="btn-primary"
                    type="object"
                    invisible="state != 'sent'"
                    context="{'override_ex': True}"
                />
            </button>
        </field>
    </record>

</odoo>
