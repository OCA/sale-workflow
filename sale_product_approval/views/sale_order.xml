<odoo>
    <record id="sale_order_line_inherit_form" model="ir.ui.view">
        <field name="name">sale_order_line_form_view_inherit</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form" />
        <field name="arch" type="xml">
            <xpath
                expr="//field[@name='order_line']//tree/field[@name='name']"
                position="after"
            >
                <button
                    attrs="{'invisible':[('approved_sale','=',True)]}"
                    class="fa fa-exclamation-triangle"
                    title="Product has been marked as not saleable."
                />
                <field invisible="1" name="approved_sale" />
            </xpath>
            <div name="button_box" position="after">
                <field invisible="1" name="exceptions_sale_approval" />
                <div
                    attrs="{'invisible':[('exceptions_sale_approval','=',False)]}"
                    class="alert alert-warning text-center"
                    role="alert"
                >Exception: A product on this sale order has been marked as not saleable.</div>
            </div>
        </field>
    </record>
</odoo>
