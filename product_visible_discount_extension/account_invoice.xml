<?xml version="1.0" encoding="utf-8"?>                                                                                                      
<openerp>
    <data>

        <record id="invoice_form" model="ir.ui.view">
            <field name="name">account.invoice.form</field>
            <field name="model">account.invoice</field>
            <field name="inherit_id" ref="account.invoice_form"/>
            <field name="arch" type="xml">

                <data>

                    <xpath expr="//notebook//tree/field[@name='price_unit']"
                           position="before"
                           >
                        <field name="list_price" />
                    </xpath>

                    <xpath expr="//notebook//tree/field[@name='discount']"
                           position="replace"
                           >
                        <field name="visible_discount" />
                    </xpath>

                </data>

            </field>
        </record>

        <template id="report_invoice_document" inherit_id="account.report_invoice_document">
            <xpath expr="//tr[@t-foreach='o.invoice_line']/td[@groups='sale.group_discount_per_so_line']"
                   position="replace">

                <td class="text-right" groups="sale.group_discount_per_so_line">
                    <span t-field="l.visible_discount"/>
                </td>

            </xpath>
        </template>

    </data>
</openerp>
