<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="sale.res_config_settings_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//setting[@id='order_warnings']" position="after">
                <setting
                    id="use_invoice_commercial_partner_filter"
                    string="Filter Invoice Address by Customer’s Commercial Partner"
                    help="If enabled, the invoice address can only be selected from partners whose commercial partner is the same as the sales order’s customer."
                >
                    <field name="use_invoice_commercial_partner_filter" />
                </setting>
                <setting
                    id="use_shipping_commercial_partner_filter"
                    string="Filter Shipping Address by Customer’s Commercial Partner"
                    help="If enabled, the delivery address can only be selected from partners whose commercial partner is the same as the sales order’s customer."
                >
                    <field name="use_shipping_commercial_partner_filter" />
                </setting>
            </xpath>
        </field>
    </record>
</odoo>
