Go to *Sales \> Configuration \> Settings* and enable *Delivery Date Required*
in the *Quotations & Orders* section. The setting applies per company.

From then on, an order cannot be confirmed while its delivery date, in the order
header, is empty.
