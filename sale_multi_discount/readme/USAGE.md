On a sale order line, click the discount cell to open the distribution
editor. Add as many discount rows as needed; each is applied on the result
of the previous one. The aggregated percentage is shown next to the tags
as a small ``= XX.XX%`` indicator and stored in the ``discount`` field so
that subtotals, taxes and reports keep working.