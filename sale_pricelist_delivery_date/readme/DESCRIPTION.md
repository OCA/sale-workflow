This module allows users to calculate the unit price of sale order lines based on the
delivery date of the sales order. The delivery date is taken from the sales order’s
commitment date (`commitment_date`) when set; otherwise, it falls back to the expected
date (`expected_date`).
