Go to **Sales > Configuration > Settings** and look for the
*Delivery Requests* section to configure:

- **Expiration Days**: Number of calendar days after the Planning response
  date before a confirmed delivery request expires (default: 15).

To grant access to the delivery request workflow, assign users to the
**Delivery Requests / User** group under **Settings > Users & Companies >
Groups**.
