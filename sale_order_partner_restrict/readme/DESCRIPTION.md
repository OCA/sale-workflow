Automatically cancels sale orders based on partner field values.

Configure rules to block orders when specific partner field values match
your blocked list (email, phone, ZIP, VAT, etc.).

When creating a sale order (UI, API, import, integration), if the partner's
field value is in the blocked list, the order is automatically canceled with
a custom message posted in the chatter.
