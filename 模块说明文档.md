# Odoo销售工作流模块详细说明

本文档详细介绍了 `/home/max/projects/odoo-addons/sale-workflow` 目录下各个模块的功能和用途。

## 1. sale_advance_payment（销售预付款）

该模块允许在销售订单中添加预付款，并在发票中使用这些预付款。

### 主要功能：
- 在销售订单中添加预付款功能
- 预付款可以用于后续生成的发票
- 提供直观的操作界面，通过"Pay Sale Advance"按钮进行操作
- 在生成发票时，系统会显示预付款项，用户可以选择将其添加到发票中

## 2. sale_automatic_workflow（销售自动工作流）

此模块创建自动化工作流，简化销售订单处理过程。

### 主要功能：
- 支持设置默认值，如配送策略、发票政策、发票日期等
- 自动执行操作，如订单验证、创建发票、发票验证等
- 特别适用于电商平台集成，可以减少人工干预
- 可以为不同的业务场景配置不同的工作流

## 3. sale_blanket_order（一揽子销售订单）

实现一揽子订单功能，支持创建长期有效的预协议销售订单。

### 主要功能：
- 创建预协议销售订单，可用于多次后续订单
- 支持按数量和有效期生成后续销售订单
- 可以为每个订单行设置不同的截止日期
- 提供专门的视图来管理一揽子订单及其相关订单

## 4. sale_commercial_partner（销售商业实体）

在销售订单上添加"商业实体"关联字段。

### 主要功能：
- 添加商业实体字段，用于关联公司级别的合作伙伴
- 在报价单、订单和销售报表中支持按商业实体分组
- 帮助企业更好地跟踪和管理与大客户的业务关系

## 5. sale_fixed_discount（销售固定折扣）

允许在销售订单行应用固定金额折扣。

### 主要功能：
- 支持在销售订单行中应用固定金额折扣（而非百分比折扣）
- 扩展销售订单报表以显示固定折扣信息
- 依赖于 account_invoice_fixed_discount 模块

## 6. sale_manual_delivery（销售手动交货）

允许手动创建交货单，而不是在确认销售订单时自动创建。

### 主要功能：
- 销售订单确认后不会直接影响库存
- 允许根据实际需要手动创建交货单
- 适用于长期项目，可以分批次发货
- 每次创建新的交货单时，可以选择不同的承运商和计划日期

## 7. sale_order_line_input（销售订单行输入）

扩展销售功能，允许直接处理销售订单行。

### 主要功能：
- 提供专门的销售订单行视图
- 支持过滤、分组和切换视图类型
- 可以创建与现有订单关联的新订单行
- 如果订单字段为空，系统会自动创建新订单

## 8. sale_order_lot_generator（销售订单批次生成器）

在确认销售订单时为每个销售行自动生成批号。

### 主要功能：
- 自动生成唯一的批号
- 需要在产品库存标签中勾选"Auto Generate Lot"字段
- 简化批次管理流程，特别适用于需要追踪产品批次的行业

## 9. sale_order_lot_selection（销售订单批次选择）

允许在销售订单行中选择特定的批次编号。

### 主要功能：
- 在销售订单行中选择批次编号
- 确保交付给客户的商品是选定的批次
- 与产品的批次追踪功能配合使用

## 10. sale_order_product_recommendation（销售订单产品推荐）

基于客户历史购买记录提供产品推荐。

### 主要功能：
- 添加产品推荐向导，基于客户近期交付的产品生成推荐
- 支持配置价格来源、时间范围、推荐数量等参数
- 可以一键将推荐产品添加到销售订单中
- 提高销售效率，增加交叉销售机会

## 11. sale_product_set（销售产品套装）

帮助销售人员快速创建多个销售订单行。

### 主要功能：
- 定义产品套装（包含多个产品及其数量）
- 销售人员可以快速将整个套装添加到报价单中
- 添加后，每个订单行都可以像普通订单行一样更新或删除
- 提高处理重复性订单的效率

## 12. sale_promotion_rule（销售促销规则）

在销售订单中添加促销规则概念。

### 主要功能：
- 支持两种类型的促销规则：自动规则和优惠券
- 自动规则在点击"应用折扣"按钮时自动计算
- 优惠券规则具有优先级，可以覆盖自动规则
- 提供可视化的促销规则管理界面

## 13. sale_start_end_dates（销售起止日期）

在销售订单行中添加开始和结束日期字段。

### 主要功能：
- 添加"开始日期"和"结束日期"字段到销售订单行
- 当产品具有"必须有开始和结束日期"属性时，这些字段为必填
- 在创建发票时，将销售订单行的起止日期复制到发票行
- 作为技术模块，设计用于与其他模块（如租赁模块）配合使用

## 14. sale_stock_picking_blocking（销售库存拣货阻断）

允许阻止从销售订单创建交货单并提供原因。

### 主要功能：
- 阻止销售订单自动创建交货单
- 提供阻断原因说明
- 支持按客户设置默认阻断原因
- 支持按付款条件设置默认阻断原因
- 可以授权特定用户解除阻断

## 15. sale_stock_picking_note（销售库存拣货备注）

扩展销售订单以包含外部（客户）和内部拣货备注。

### 主要功能：
- 在销售订单中添加外部（客户）和内部拣货备注字段
- 备注信息会传递到相应的拣货单
- 支持从客户主数据中继承默认备注信息
- 在拣货单的"Comments"标签中显示备注信息

## 16. partner_contact_sale_info_propagation（合作伙伴联系人销售信息传播）

将公司的销售人员和销售团队信息传播到联系人。

### 主要功能：
- 将父公司的销售人员和销售团队信息传播到联系人
- 当联系人没有相关信息时，自动填充父公司的信息
- 当公司信息变更时，自动更新符合条件的联系人信息

## 17. partner_sale_pivot（合作伙伴销售透视表）

扩展销售模块功能，在客户表单视图中显示销售分析。

### 主要功能：
- 在客户表单视图中添加销售分析智能按钮
- 按产品行和确认日期列显示该客户的销售分析透视表
- 仅对具有销售访问权限的用户可见

## 18. portal_sale_personal_data_only（门户销售个人数据限制）

限制门户用户只能看到自己的销售订单。

### 主要功能：
- 门户用户只能访问属于其合作伙伴或下属合作伙伴的销售订单
- 防止组织内不同部门之间的文档泄露
- 关联的发票也会对相应门户用户可见

## 19. product_form_sale_link（产品表单销售链接）*

在产品表单中添加指向销售订单行的链接。

### 主要功能：
- 在产品表单中添加"销售"按钮
- 点击按钮可以直接查看该产品的所有销售订单行
- 方便查看产品的销售历史和趋势

## 20. product_supplierinfo_for_customer_sale（客户产品信息销售）

基于客户产品信息加载销售订单中的客户代码。

### 主要功能：
- 在销售订单中加载产品为客户定义的代码
- 支持使用每个产品中配置的客户产品代码和名称
- 可以根据客户价格信息中的最小数量建议销售数量
- 如果产品没有为客户配置特定信息，则使用默认代码搜索