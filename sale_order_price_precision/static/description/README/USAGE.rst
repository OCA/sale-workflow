After installing this module:

* Sale order line prices will use the "Product Price Sales" decimal precision
* The precision can be configured in Settings > Technical > Database Structure > Decimal Accuracy
* Default precision is set to 2 decimal places but can be adjusted
