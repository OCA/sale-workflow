This module shows the *Routes* column by default on the sale order line
list in quotations and sales orders.

The `route_ids` column is provided by `sale_stock` as an optional
(hidden) column, so every user has to enable it manually. This module
sets that column to be shown by default instead.
