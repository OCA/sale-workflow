Set a value on the **Reference** field of any contact (`res.partner.ref`).

Open the **Sales** app, create or edit a quotation and open the **Customer**
dropdown — matching partners appear with the reference prefix:

    [C00123] Acme Corp

The prefix stays on the selected value too, so the reference remains visible
on the saved order. Views outside Sales keep the plain partner name.
