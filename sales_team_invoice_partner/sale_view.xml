<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>

        <record id="view_order_form" model="ir.ui.view">
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="arch" type="xml">
                <field name="partner_invoice_id" position="after">
                    <field name="section_partner_invoice_id" attrs="{'invisible':['|', ('partner_invoice_id', '!=', False), ('section_partner_invoice_id', '=', False)]}"/>
                </field>
                <field name="partner_invoice_id" position="attributes">
                    <attribute name="attrs">{'readonly':[('section_partner_invoice_id','!=', False)], 'invisible':[('partner_invoice_id', '=', False), ('section_partner_invoice_id', '!=', False)]}</attribute>
                </field>
            </field>
        </record>

    </data>
</openerp>
