This module lets you define a default sale order priority per customer which will be populated on sale.order

It builds on `sale_order_priority`, which adds a *Priority* (*Normal* /
*Urgent*) on sale orders and their lines, and computes the order priority as
the maximum priority of its lines.
