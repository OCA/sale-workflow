To set the default priority of a customer:

1. Go to *Sales > Orders > Customers* and open a customer.
2. In the *Sales & Purchase* tab, set the *Sale Priority* field.

The field is a commercial field, so a company and its contacts always share
the same value.

Leaving it empty keeps the standard behaviour of `sale_order_priority`.
