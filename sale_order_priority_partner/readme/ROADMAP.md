- The customer priority is applied when the order is created, so the value is
  not visible in the quotation form before it is saved.
- The customer priority takes precedence over the priority set on the order
  at creation time: an order created for a customer having a *Sale Priority*
  always gets that priority.
