This module allows assigning a dedicated `ir.sequence` to each sales team.
When a sale order is created for a team that has a sequence configured, that
sequence is used to generate the order name instead of the global sale order
sequence.
