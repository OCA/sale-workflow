Extends the product catalog in Sales Orders to enforce secondary unit usage:
when a product has a dependent default secondary unit configured, the catalog
displays and accepts quantities in that secondary unit instead of the primary one.
