This module allows to source a sale order line from a specific warehouse
instead of always using the warehouse of the sale order.

A separate delivery order is generated for each warehouse set on the
sale order lines.
